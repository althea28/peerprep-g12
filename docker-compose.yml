services:
  matching-service:
    build: ./matching-service
    ports:
      - "3002:3002"
    environment:
      - PORT=3002
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis

  # Redis database for matching queue
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"